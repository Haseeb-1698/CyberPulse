# Secrets and environment variables
.env
.secrets

# Python cache files
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# celery beat schedule file
celerybeat-schedule

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Large data files and caches (handled by Git LFS)
# *.pkl - Now tracked by Git LFS
# *.csv - Now tracked by Git LFS  
# *.xml - Now tracked by Git LFS
# exploitdb/** - Now tracked by Git LFS
# models/** - Now tracked by Git LFS
# static/data/** - Now tracked by Git LFS

# Cache directories (now tracked by Git LFS)
# cve_cache/ - Now tracked by Git LFS
# mitre_cache/ - Now tracked by Git LFS
# remediation_cache/ - Now tracked by Git LFS
# exploit_cache/ - Now tracked by Git LFS
# vulners_cache/ - Now tracked by Git LFS
# shodan_cache/ - Now tracked by Git LFS

# Log files (now tracked by Git LFS)
# *.log - Now tracked by Git LFS
logs/

# Upload files (now tracked by Git LFS)
# uploads/ - Now tracked by Git LFS

# Generated reports (now tracked by Git LFS)
# integrated_results/ - Now tracked by Git LFS
# scans/ - Now tracked by Git LFS

# Dashboard data (now tracked by Git LFS)
# dashboard_data.json - Now tracked by Git LFS
# jobs_persistence.json - Now tracked by Git LFS
# scan_history.json - Now tracked by Git LFS

# Test files (now tracked by Git LFS)
# test_vulnerabilities.json - Now tracked by Git LFS

# Remediation feedback (now tracked by Git LFS)
# remediation_feedback/ - Now tracked by Git LFS

# JSON files (now tracked by Git LFS, except essential config files)
# *.json - Now tracked by Git LFS
!requirements.txt
!config.json
!README.md

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db 